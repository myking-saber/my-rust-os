[workspace]
members = ["kernel"]

[package]
name = "my-os"  # 注意這裡，應該是 my-os 而不是 my-kernel
version = "0.1.0"
edition = "2021"

[build-dependencies]
bootloader = "0.11"
kernel = { path = "kernel", artifact = "bin", target = "x86_64-unknown-none" }